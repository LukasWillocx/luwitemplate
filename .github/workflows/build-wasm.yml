on:
  push:
    branches: [main]
  workflow_dispatch:

name: Build WASM

jobs:
  build:
    uses: r-wasm/actions/.github/workflows/build-and-deploy.yml@v1
    permissions:
      contents: read
      pages: write
      id-token: write
